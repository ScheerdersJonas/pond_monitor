[project]
name = "pond-monitor"
version = "0.1.0"
description = "edge-to-cloud IoT pipeline with local buffering and cloud sync based on my pond's water pump"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/pond_monitor"]
